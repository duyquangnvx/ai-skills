# yaml-language-server: $schema=http://json-schema.org/draft-07/schema#
# JSON Schema for UI Blueprint frontmatter and YAML islands.
# Copy into the project (e.g. ui-blueprints/_schema/blueprint.schema.yaml) and
# wire into a validator script if linting is wanted.
#
# This is the universal schema. Project-specific extensions (custom widgets,
# project verbs, project namespaces) are layered on via _config.md.

$schema: http://json-schema.org/draft-07/schema#
title: UIBlueprint
type: object
description: Validates a UI blueprint document split into frontmatter + YAML island parts.
required: [frontmatter, ui, modes, acceptance]

properties:

  # ---- Frontmatter ----
  frontmatter:
    type: object
    required: [id, type, title]
    properties:
      id:
        type: string
        pattern: "^[a-zA-Z][a-zA-Z0-9_]*$"
      type:
        enum: [scene, popup, shared]
      title:
        type: string
      orientation:
        enum: [portrait, landscape, both]
      safeArea:
        type: boolean
      modal:
        type: boolean
      dismissible:
        type: boolean
      parents:
        type: array
        items: { type: string }
      children:
        type: array
        items: { type: string }
      sources:
        type: array
        items: { type: string }
      dataBindings:
        type: array
        items:
          type: object
          additionalProperties: { type: string }
      emits:
        type: array
        items: { type: string }
      listens:
        type: array
        items: { type: string }
    allOf:
      # modal/dismissible only valid for popup
      - if:
          properties: { type: { const: scene } }
        then:
          properties:
            modal: { not: {} }
            dismissible: { not: {} }
      - if:
          properties: { type: { const: shared } }
        then:
          properties:
            modal: { not: {} }
            dismissible: { not: {} }

  # ---- ## ui island (nested tree) ----
  ui:
    $ref: "#/definitions/uiNode"

  # ---- ## modes island ----
  modes:
    oneOf:
      - type: array
        minItems: 1
        items:
          type: object
          required: [id]
          properties:
            id:
              type: string
              pattern: "^[a-zA-Z][a-zA-Z0-9_]*$"
            description: { type: string }
            initial: { type: boolean }
            final: { type: boolean }
            enter:
              type: object
              properties:
                do:
                  type: array
                  items: { type: string }
            exit:
              type: object
              properties:
                do:
                  type: array
                  items: { type: string }
            on:
              type: array
              items:
                type: object
                properties:
                  widget: { type: string }
                  event:  { type: string }
                  where:  { type: string }
                  do:
                    type: array
                    items: { type: string }
                  goto:
                    type: string
                anyOf:
                  - required: [widget]
                  - required: [event]
      - const: _none_

  # ---- ## acceptance island ----
  acceptance:
    oneOf:
      - type: array
        items:
          type: object
          required: [id, given, when, then]
          properties:
            id:
              type: string
              pattern: "^U[A-Za-z0-9_-]+$"
            given: { type: string }
            when: { type: string }
            then: { type: string }
            test_hint: { type: string }
      - const: _none_

definitions:

  uiNode:
    type: object
    required: [type]
    properties:
      id:
        type: string
        pattern: "^[a-zA-Z][a-zA-Z0-9_]*$"
      type:
        oneOf:
          # Container types (and Spacer leaf)
          - enum: [VStack, HStack, ZStack, Grid, Scroll, Spacer]
          # Widget types
          - enum:
              - Text
              - Image
              - Icon
              - Button
              - IconButton
              - Toggle
              - Slider
              - ProgressBar
              - List
              - HitArea
              - Custom
      # Sizing
      width:  { $ref: "#/definitions/sizingUnit" }
      height: { $ref: "#/definitions/sizingUnit" }
      flex:   { type: integer, minimum: 1 }
      align:  { $ref: "#/definitions/alignNine" }
      offset:
        type: object
        properties:
          x: { $ref: "#/definitions/sizingUnit" }
          y: { $ref: "#/definitions/sizingUnit" }
      # Bindings / interactions
      bind:
        type: object
        additionalProperties: true
      visible:
        type: object
        required: [bind]
        properties:
          bind: { type: string }
      enabled:
        type: object
        required: [bind]
        properties:
          bind: { type: string }
      on:
        type: object
        additionalProperties:
          type: array
          items: { type: string }
      style:
        type: object
        additionalProperties: true
      # Container nesting
      children:
        type: array
        items: { $ref: "#/definitions/uiNode" }
      # Container-specific options
      cols: { type: integer, minimum: 1 }
      rows: { oneOf: [{ type: integer, minimum: 1 }, { const: auto }] }
      gap:  { $ref: "#/definitions/sizingUnit" }
      axis: { enum: [vertical, horizontal] }
      # Widget-specific props (universal subset; project widgets layer more in _config.md)
      text:    { type: string }
      label:   { type: string }
      icon:    { type: string }
      asset:   { type: string }
      fit:     { enum: [contain, cover, fill] }
      tint:    { type: string }
      size:    { $ref: "#/definitions/sizingUnit" }
      maxLines: { type: integer, minimum: 1 }
      min:     { type: number }
      max:     { type: number }
      step:    { type: number }
      variant: { type: string }
      separator: { type: boolean }
      badge:
        type: object
        additionalProperties: true
      name:    { type: string }
      props:
        type: object
        additionalProperties: true
      itemTemplate:
        $ref: "#/definitions/uiNode"
    allOf:
      # Custom requires `name`
      - if:
          properties: { type: { const: Custom } }
        then:
          required: [name]
      # ZStack children require `align`
      # (note: this is a parent-side rule; enforced by a companion script,
      #  not expressible cleanly in JSON Schema draft-7 since it's about siblings)

  sizingUnit:
    type: string
    # Matches: 70dp, 100%w, 100%h, 50%sw, 50%sh, auto, fill, min(...), max(...)
    # Compound expressions inside min()/max() validated by a companion script.
    pattern: "^([0-9]+(\\.[0-9]+)?(dp|%w|%h|%sw|%sh)|auto|fill|min\\([^)]+\\)|max\\([^)]+\\))$"

  alignNine:
    enum:
      - top-left
      - top-center
      - top-right
      - center-left
      - center
      - center-right
      - bottom-left
      - bottom-center
      - bottom-right
