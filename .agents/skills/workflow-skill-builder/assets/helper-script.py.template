"""{{ONE_LINE_PURPOSE}}.

Usage:
    python {{SCRIPT_NAME}}.py --input {{INPUT_HINT}} --output {{OUTPUT_HINT}}

Example:
    {{EXAMPLE_CALL}}
"""

from __future__ import annotations

import argparse
import json
from pathlib import Path


def {{FUNCTION_NAME}}({{ARG_LIST}}) -> {{RETURN_TYPE}}:
    """{{ONE_LINE_FUNCTION_PURPOSE}}.

    Args:
        {{ARG_DOC}}

    Returns:
        {{RETURN_DOC}}

    Raises:
        {{RAISES_DOC}}
    """
    raise NotImplementedError(
        "Domain logic — implement based on: {{ONE_LINE_PURPOSE}}"
    )


def _write_atomic(path: Path, payload: object) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    tmp = path.with_suffix(path.suffix + ".tmp")
    tmp.write_text(json.dumps(payload, ensure_ascii=False, indent=2), encoding="utf-8")
    tmp.replace(path)


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--input", type=Path, required=True)
    parser.add_argument("--output", type=Path, required=True)
    args = parser.parse_args()

    try:
        result = {{FUNCTION_NAME}}(args.input)
    except Exception as exc:
        error_path = args.output.with_suffix(args.output.suffix + ".error")
        error_path.parent.mkdir(parents=True, exist_ok=True)
        error_path.write_text(f"{type(exc).__name__}: {exc}\n", encoding="utf-8")
        raise SystemExit(1) from exc

    _write_atomic(args.output, result)


if __name__ == "__main__":
    main()
