# Progress

<!-- Regenerated rollup, never hand-written prose. Derive every row from
docs/backlog.md + story-packet frontmatter; packets are the truth, this file
is the cache for fast resume. One row per in-flight story; in a parallel
project also one row per claimed epic (Story empty until sliced) — the
Assignee column is where epic claims live. Regenerate whole-file at
serialized points: session end (single session) or claim/merge (parallel).
No backlog in this repo → replace the table with three overwritten lines:
Now / Next / Blocked. -->

| Epic | Story | Status | Assignee | Blocked on |
|------|-------|--------|----------|------------|
| E0X | US-XXX | in progress | — | — |

- Next: <top candidate story or the next epic to slice, from docs/backlog.md>
