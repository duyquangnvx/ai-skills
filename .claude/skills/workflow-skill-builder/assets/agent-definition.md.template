---
name: {{AGENT_NAME}}
description: {{ONE_LINE_AGENT_DESCRIPTION}}
tools: {{TOOL_LIST}}
model: {{MODEL}}
permissionMode: {{PERMISSION_MODE}}
---

# Role: {{AGENT_TITLE}}

## Purpose

{{ONE_PARAGRAPH_PURPOSE}}

## Input

You receive a prompt referencing {{INPUT_REFERENCE}}. Read the input from `{{INPUT_PATH}}`.

## Output

Write exactly one file:

- Success: `{{OUTPUT_PATH_PATTERN}}` — JSON matching the schema below.
- Failure: `{{OUTPUT_PATH_PATTERN}}.error` — plain text with the error message.

Write atomically:

```bash
echo "$RESULT" > "$OUT.tmp" && mv "$OUT.tmp" "$OUT"
```

## Procedure

1. {{STEP_1}}
2. {{STEP_2}}
3. {{STEP_3}}

## Output schema

```json
{{JSON_SCHEMA_OR_STRUCTURE}}
```

{{IF_JUDGMENT_AGENT_ANTI_CODING_BLOCK}}

<!--
RENDER NOTES (delete after filling):

If this is a judgment agent (reads input and produces a decision), substitute
IF_JUDGMENT_AGENT_ANTI_CODING_BLOCK with:

## Anti-coding rule

At no point should you Write a script to perform the analysis. If you think
you need to write code to answer the question, you have misunderstood — re-read
the input and reason about it directly.

If this is a mechanical agent (calls helpers, transforms files), delete the
IF_JUDGMENT_AGENT_ANTI_CODING_BLOCK placeholder entirely.
-->
